(function() {
  'use strict';

  function inputGroupController($scope, $location) {
    
  }

  const dependencies = ['$scope'];
  dependencies.push('$location');
  dependencies.push(inputGroupController);
  app.controller('inputGroupCtrl', dependencies);
}());
